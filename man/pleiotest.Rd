% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pleiotest.R
\name{pleiotest}
\alias{pleiotest}
\title{Run Comprehensive Pleiotropy Analysis}
\usage{
pleiotest(
  pleio_object,
  run_amatz_test = TRUE,
  run_cmats_test = TRUE,
  run_emats_test = TRUE,
  run_dsum_test = TRUE,
  run_metacca_test = TRUE,
  run_minp_test = TRUE,
  run_mixada_test = TRUE,
  run_mixfisher_davies_test = TRUE,
  run_mixfisher_liu_test = TRUE,
  run_mixfisher_liumod_test = TRUE,
  run_mixsd_davies_test = TRUE,
  run_mixsd_liu_test = TRUE,
  run_mixsd_liumod_test = TRUE,
  run_mixtippett_davies_test = TRUE,
  run_mixtippett_liu_test = TRUE,
  run_mixtippett_liumod_test = TRUE,
  run_mixvar_davies_test = TRUE,
  run_mixvar_liu_test = TRUE,
  run_mixvar_liumod_test = TRUE,
  run_pcaq_test = FALSE,
  run_pcfisher_test = TRUE,
  run_pclc_test = TRUE,
  run_pcminp_test = TRUE,
  run_pco_test = FALSE,
  run_sum_test = TRUE,
  run_tates_test = TRUE,
  run_vc_test = TRUE,
  run_wald_test = TRUE,
  run_wi_test = TRUE
)
}
\arguments{
\item{pleio_object}{An object of class `pleio`, containing summary statistics for genetic variants.}

\item{run_amatz_test}{Logical; whether to run the AMATZ test. Default is `TRUE`.}

\item{run_cmats_test}{Logical; whether to run the CMATS test. Default is `TRUE`.}

\item{run_emats_test}{Logical; whether to run the EMATS test. Default is `TRUE`.}

\item{run_dsum_test}{Logical; whether to run the DSUM test. Default is `TRUE`.}

\item{run_metacca_test}{Logical; whether to run the metaCCA test. Default is `TRUE`.}

\item{run_minp_test}{Logical; whether to run the MinP test. Default is `TRUE`.}

\item{run_mixada_test}{Logical; whether to run the MixAda test. Default is `TRUE`.}

\item{run_mixfisher_davies_test}{Logical; whether to run the MixFisher test (Davies approximation). Default is `TRUE`.}

\item{run_mixfisher_liu_test}{Logical; whether to run the MixFisher test (Liu approximation). Default is `TRUE`.}

\item{run_mixfisher_liumod_test}{Logical; whether to run the MixFisher test (Liu-modified approximation). Default is `TRUE`.}

\item{run_mixsd_davies_test}{Logical; whether to run the MixSD test (Davies approximation). Default is `TRUE`.}

\item{run_mixsd_liu_test}{Logical; whether to run the MixSD test (Liu approximation). Default is `TRUE`.}

\item{run_mixsd_liumod_test}{Logical; whether to run the MixSD test (Liu-modified approximation). Default is `TRUE`.}

\item{run_mixtippett_davies_test}{Logical; whether to run the MixTippett test (Davies approximation). Default is `TRUE`.}

\item{run_mixtippett_liu_test}{Logical; whether to run the MixTippett test (Liu approximation). Default is `TRUE`.}

\item{run_mixtippett_liumod_test}{Logical; whether to run the MixTippett test (Liu-modified approximation). Default is `TRUE`.}

\item{run_mixvar_davies_test}{Logical; whether to run the MixVar test (Davies approximation). Default is `TRUE`.}

\item{run_mixvar_liu_test}{Logical; whether to run the MixVar test (Liu approximation). Default is `TRUE`.}

\item{run_mixvar_liumod_test}{Logical; whether to run the MixVar test (Liu-modified approximation). Default is `TRUE`.}

\item{run_pcaq_test}{Logical; whether to run the PCAQ test. Default is `FALSE`.}

\item{run_pcfisher_test}{Logical; whether to run the PCFisher test. Default is `TRUE`.}

\item{run_pclc_test}{Logical; whether to run the PCLC test. Default is `TRUE`.}

\item{run_pcminp_test}{Logical; whether to run the PCMinP test. Default is `TRUE`.}

\item{run_pco_test}{Logical; whether to run the PCO test. Default is `FALSE`.}

\item{run_sum_test}{Logical; whether to run the SUM test. Default is `TRUE`.}

\item{run_tates_test}{Logical; whether to run the TATES test. Default is `TRUE`.}

\item{run_vc_test}{Logical; whether to run the Variance Component (VC) test. Default is `TRUE`.}

\item{run_wald_test}{Logical; whether to run the Wald test. Default is `TRUE`.}

\item{run_wi_test}{Logical; whether to run the WI (Weighted Integration) test. Default is `TRUE`.}
}
\value{
An object of class `pleiot` containing the aggregated test results and execution times.
}
\description{
This function runs multiple pleiotropy tests on a given `pleio` object, aggregating results
from various statistical methods. It allows for flexible selection of tests to be executed.
}
\details{
The `pleiotest` function systematically applies multiple statistical tests to evaluate pleiotropy
using a `pleio` object containing summary statistics from a GWAS-like study. The function supports
multiple pleiotropy detection methods, including `MixFisher`, `MixVar`, `MixTippett`, and `PCMinP`,
among others. Execution time for each test is recorded.

The function returns an object of class `pleiot`, containing:
- A summary table of results from all selected tests
- Execution times for each test
}
\examples{
# Simulating a pleiotropic dataset
library(pleiosim)
sim_data <- pleiosim::simulate_pleiotropy(n_snps = 1000, n_traits = 5, n_samples = 500)

# Running pleiotropy tests
results <- pleiotest(sim_data, run_mixfisher_davies_test = TRUE, run_minp_test = TRUE)

# View results
head(results@pleiotest_stats)
results@execution_time

}
